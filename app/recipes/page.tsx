import type{Metadata}from"next";import Image from"next/image";import Link from"next/link";import{getPublishedRecipes}from"@/lib/recipes/public";export const metadata:Metadata={title:"Recipe Book",description:"Explore published Vijaya Premix recipes.",alternates:{canonical:"/recipes"}};export default async function Page(){const recipes=await getPublishedRecipes();return <section className="mx-auto max-w-[1400px] px-4 py-12 sm:px-6 lg:px-8"><div className="mb-10"><h1 className="font-display text-4xl font-bold text-vijaya-red sm:text-5xl">Recipe Book</h1><p className="mt-2 text-vijaya-muted">Serving ideas made with Vijaya Premix.</p></div>{recipes.length?<div className="grid gap-6 sm:grid-cols-2 lg:grid-cols-3">{recipes.map(r=><article key={r.id} className="overflow-hidden rounded-4xl bg-white shadow-soft"><div className="relative h-56 bg-vijaya-pink">{(r.imagePath||r.product?.imagePath)&&<Image src={r.imagePath||r.product!.imagePath} alt={r.title} fill className="object-contain p-4" unoptimized/>}</div><div className="p-5"><p className="text-xs font-bold uppercase text-vijaya-red">{r.category??"Recipe"}</p><h2 className="font-display text-2xl font-bold">{r.title}</h2><p className="mt-2 text-sm text-vijaya-muted">{r.shortDescription??r.description}</p><Link href={`/recipes/${r.slug}`} className="mt-5 inline-block rounded-full border-2 border-vijaya-red px-5 py-2.5 font-bold text-vijaya-red">View Recipe</Link></div></article>)}</div>:<p className="rounded-4xl bg-white p-12 text-center text-vijaya-muted">Published recipes will appear here.</p>}</section>}
