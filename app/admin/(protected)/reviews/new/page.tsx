import type {Metadata} from "next";import {db} from "@/lib/db";import {ReviewForm} from "@/components/admin/ReviewForm";import {createReview} from "../actions";export const metadata:Metadata={title:"Create Review | Admin"};export default async function Page(){const products=await db.product.findMany({select:{id:true,name:true},orderBy:{name:"asc"}});return <section className="max-w-4xl"><p className="text-xs font-extrabold uppercase tracking-[.18em] text-vijaya-gold">Reviews CMS</p><h1 className="font-display text-4xl font-bold text-vijaya-red">Create Review</h1><p className="mb-7 mt-1 text-sm text-vijaya-muted">No public review submission is enabled; add verified business feedback here.</p><ReviewForm action={createReview} products={products}/></section>}
