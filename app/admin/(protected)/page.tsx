import type{Metadata}from"next";import Link from"next/link";import{ProductStatus,RecipeStatus,ReviewStatus}from"@prisma/client";import{db}from"@/lib/db";export const metadata:Metadata={title:"Admin Dashboard"};export default async function Page(){const[published,drafts,preorders,reviews,recipes]=await Promise.all([db.product.count({where:{status:ProductStatus.PUBLISHED}}),db.product.count({where:{status:ProductStatus.DRAFT}}),db.product.count({where:{status:ProductStatus.PUBLISHED,preorder:true}}),db.review.count({where:{status:ReviewStatus.PUBLISHED}}),db.recipe.count({where:{status:RecipeStatus.PUBLISHED}})]);return <section><div className="rounded-4xl bg-white p-7 shadow-soft"><p className="text-xs font-bold uppercase tracking-widest text-vijaya-gold">Vijaya Premix Admin</p><h1 className="font-display text-4xl font-bold text-vijaya-red">Dashboard</h1><p className="text-sm text-vijaya-muted">Database-backed products, reviews, recipes, and business content.</p></div><div className="mt-6 grid grid-cols-2 gap-3 lg:grid-cols-5">{[[published,"Published Products"],[drafts,"Draft Products"],[preorders,"Preorders"],[reviews,"Reviews"],[recipes,"Recipes"]].map(([count,label])=><article key={label} className="rounded-3xl bg-white p-5 text-center shadow-soft"><p className="font-display text-3xl font-bold text-vijaya-red">{count}</p><p className="text-xs font-bold text-vijaya-muted">{label}</p></article>)}</div><div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{[["Products","/admin/products"],["Reviews","/admin/reviews"],["Recipes","/admin/recipes"],["Homepage","/admin/homepage"],["Site Settings","/admin/settings"]].map(([label,href])=><Link key={href} href={href} className="rounded-4xl bg-white p-6 font-display text-xl font-bold shadow-soft transition hover:-translate-y-1 hover:text-vijaya-red">Manage {label}</Link>)}</div></section>}
