import {db} from "@/lib/db";import {RecipeForm} from "@/components/admin/RecipeForm";import {createRecipe} from "../actions";export default async function Page(){const products=await db.product.findMany({select:{id:true,name:true},orderBy:{name:"asc"}});return <section className="max-w-4xl"><h1 className="mb-7 font-display text-4xl font-bold text-vijaya-red">Create Recipe</h1><RecipeForm action={createRecipe} products={products}/></section>}
