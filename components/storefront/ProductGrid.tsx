"use client";import {useSearchParams} from "next/navigation";import {useState} from "react";import {matchesProduct,products,type ProductFilter} from "@/data/products";import {ProductCard} from "./ProductCard";
const filters:ProductFilter[]=["all","veg","non-veg","chicken","fish"];
export function ProductGrid(){const params=useSearchParams();const initial=(filters.includes(params.get("filter") as ProductFilter)?params.get("filter"):"all") as ProductFilter;const [filter,setFilter]=useState<ProductFilter>(initial),[query,setQuery]=useState(params.get("q")||"");const shown=products.filter(p=>matchesProduct(p,filter,query));return <><div className="mb-8 flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div className="flex flex-wrap gap-2" aria-label="Product filters">{filters.map(f=><button key={f} onClick={()=>setFilter(f)} aria-pressed={filter===f} className={`rounded-full px-5 py-2.5 text-sm font-bold transition ${filter===f?"bg-vijaya-red text-white":"bg-white text-vijaya-dark hover:bg-vijaya-pink"}`}>{f==="all"?"All Products":f==="non-veg"?"Non-Veg":f[0].toUpperCase()+f.slice(1)}</button>)}</div><label className="sr-only" htmlFor="product-search">Search products</label><input id="product-search" type="search" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search products" className="rounded-full border border-vijaya-red/20 bg-white px-5 py-3 text-sm focus:outline-vijaya-red"/></div><p className="mb-5 text-sm text-vijaya-muted" aria-live="polite">Showing {shown.length} of {products.length} products</p>{shown.length?<div className="grid grid-cols-2 gap-4 sm:gap-6 md:grid-cols-3 lg:grid-cols-4">{shown.map(p=><ProductCard key={p.id} product={p}/>)}</div>:<div className="rounded-4xl bg-white p-12 text-center text-vijaya-muted">No premixes match your search.</div>}</>}
