"use client";import {useEffect,useState} from "react";
export function Motion(){const [show,setShow]=useState(false);useEffect(()=>{const reduced=matchMedia("(prefers-reduced-motion: reduce)").matches;const targets=[...document.querySelectorAll<HTMLElement>("main section:not(.hero-decor) h1, main section:not(.hero-decor) h2, .product-card, .recipe-card, main article")];targets.forEach((el,i)=>{el.classList.add("reveal-ready");el.style.setProperty("--vj-delay",reduced?"0ms":`${Math.min(i*35,280)}ms`)});if(reduced||!("IntersectionObserver" in window)){targets.forEach(el=>el.classList.add("is-revealed"))}else{const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add("is-revealed");observer.unobserve(entry.target)}}),{threshold:.12});targets.forEach(el=>observer.observe(el))}const scroll=()=>setShow(scrollY>500);addEventListener("scroll",scroll,{passive:true});return()=>removeEventListener("scroll",scroll)},[]);return <button className={`${show?"grid":"hidden"} fixed bottom-6 right-6 z-40 h-11 w-11 place-items-center rounded-full bg-vijaya-red text-white shadow-card`} onClick={()=>scrollTo({top:0,behavior:"smooth"})} aria-label="Back to top">↑</button>}
