import Link from "next/link";
export interface PublicReview{id:string;customerName:string;rating:number;content:string;product:{name:string;slug:string}|null}
export function ReviewCards({reviews,title="Loved by Our Customers"}:{reviews:PublicReview[];title?:string}){if(!reviews.length)return null;return <section className="py-16"><div className="mb-8 text-center"><h2 className="font-display text-3xl font-bold text-vijaya-red sm:text-4xl">{title}</h2><p className="mt-2 text-vijaya-muted">Published feedback shared with Vijaya Premix.</p></div><div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">{reviews.map(review=><article key={review.id} className="rounded-4xl bg-white p-6 shadow-soft"><p aria-label={`${review.rating} out of 5 stars`} className="text-lg tracking-wide text-vijaya-gold">{"★".repeat(review.rating)}<span className="text-vijaya-muted/20">{"★".repeat(5-review.rating)}</span></p><blockquote className="mt-4 text-vijaya-muted">“{review.content}”</blockquote><p className="mt-5 font-display font-bold text-vijaya-dark">{review.customerName}</p>{review.product&&<Link href={`/products/${review.product.slug}`} className="mt-1 inline-block text-xs font-bold text-vijaya-red">{review.product.name}</Link>}</article>)}</div></section>}
